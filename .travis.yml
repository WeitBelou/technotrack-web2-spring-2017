language: python3

python:
  - 3.6.2

install:
  - pip install -r requirements.txt

script:
  - pytest

deploy:
  provider: heroku

  api_key: ${HEROKU_API_KEY}
  app: technotrack-rettiwt
  run: "./migrate.py migrate"

  on:
    branch: master